services:
  dev:
    image: "${DEV_IMAGE:-ghcr.io/cainiaocome/esp32-s3-keyboard-dev:master}"
    pull_policy: always
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      IDF_TARGET: esp32s3
    stdin_open: true
    tty: true
    command: ["sleep", "infinity"]
