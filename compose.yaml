services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      IDF_TARGET: esp32s3
    stdin_open: true
    tty: true
    command: ["sleep", "infinity"]
